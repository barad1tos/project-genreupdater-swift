---
phase: 05.1-dashboard-hotfix
plan: 02
type: execute
wave: 2
depends_on:
  - "05.1-01"
files_modified:
  - Packages/SharedUI/Sources/SharedUI/SharedUI.swift
  - Packages/SharedUI/Sources/SharedUI/Theme/DesignTokens.swift
  - Packages/SharedUI/Sources/SharedUI/Components/ProgressRing.swift
  - Packages/SharedUI/Sources/SharedUI/Components/ConfidenceBadge.swift
  - Packages/SharedUI/Sources/SharedUI/Components/TierBadge.swift
  - Packages/SharedUI/Sources/SharedUI/Components/EmptyStateView.swift
  - Packages/SharedUI/Sources/SharedUI/Components/PaywallOverlay.swift
  - Packages/SharedUI/Sources/SharedUI/Components/ReportsChangeLog.swift
  - Packages/SharedUI/Sources/SharedUI/Components/TrackRow.swift
  - Packages/SharedUI/Sources/SharedUI/Components/ReportsCharts.swift
  - App/Views/SubscriptionView.swift
  - App/Views/UpdateView.swift
  - App/Views/Components/DryRunSummaryView.swift
  - App/Views/Components/AlbumCard.swift
  - App/GenreUpdaterApp.swift
  - App/Views/MainView.swift
  - App/Views/UpdateWorkflowView.swift
  - App/Views/Components/GenreMappingsEditor.swift
autonomous: true
requirements:
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "No raw spacing literals exist where a Spacing token matches (except intentional sub-token values: 0, 2, 6)"
    - "No raw animation literals exist where a Motion token matches"
    - "ProgressRing uses Ayu tokens instead of system colors (.secondary, .primary, .quaternary)"
    - "SharedUI.swift stale Phase 1 comment is removed"
    - "motionAnimation extension is preserved in DesignTokens.swift (not deleted)"
    - "Motion.curveLayout token exists for the 0.25s column visibility animation"
    - "Periphery scan finds no additional dead code (or findings are documented)"
  artifacts:
    - path: "Packages/SharedUI/Sources/SharedUI/Theme/DesignTokens.swift"
      provides: "Motion.curveLayout token at 0.25s"
      contains: "curveLayout"
    - path: "Packages/SharedUI/Sources/SharedUI/SharedUI.swift"
      provides: "Updated module comment (no stale Phase 1 reference)"
    - path: "Packages/SharedUI/Sources/SharedUI/Components/ProgressRing.swift"
      provides: "Ayu token replacements for system colors and Spacing tokens for raw literals"
      contains: "Ayu.fgSecondary"
  key_links:
    - from: "App/Views/MainView.swift"
      to: "Packages/SharedUI/Sources/SharedUI/Theme/DesignTokens.swift"
      via: "Motion.curveLayout for column visibility animation"
      pattern: "Motion\\.curveLayout"
    - from: "Packages/SharedUI/Sources/SharedUI/Components/ProgressRing.swift"
      to: "Packages/SharedUI/Sources/SharedUI/Theme/AyuColors.swift"
      via: "Ayu.fgSecondary/fgPrimary/bgTertiary replacing system colors"
      pattern: "Ayu\\.(fgSecondary|fgPrimary|bgTertiary)"
---

<objective>
Perform broad token consistency sweep and dead code cleanup across SharedUI and App targets. Replace raw spacing/animation literals with DesignToken equivalents, fix ProgressRing system color usage, clean stale comments, add missing Motion tokens, and run Periphery scan.

Purpose: Full token consistency means every spacing value and animation duration references the design system -- making future theme changes propagate automatically and reducing cognitive load for contributors. The codebase should have zero raw literals where a token match exists.

Output: 18 files updated with token replacements, DesignTokens.swift extended with Motion.curveLayout, SharedUI.swift comment fixed, ProgressRing using Ayu colors, Periphery scan results documented.
</objective>

<execution_context>
@/Users/cloud/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cloud/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-dashboard-hotfix/05.1-CONTEXT.md
@.planning/phases/05.1-dashboard-hotfix/05.1-RESEARCH.md
@.planning/phases/05.1-dashboard-hotfix/05.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace raw literals with tokens across SharedUI components</name>
  <files>
    Packages/SharedUI/Sources/SharedUI/Theme/DesignTokens.swift
    Packages/SharedUI/Sources/SharedUI/SharedUI.swift
    Packages/SharedUI/Sources/SharedUI/Components/ProgressRing.swift
    Packages/SharedUI/Sources/SharedUI/Components/ConfidenceBadge.swift
    Packages/SharedUI/Sources/SharedUI/Components/TierBadge.swift
    Packages/SharedUI/Sources/SharedUI/Components/EmptyStateView.swift
    Packages/SharedUI/Sources/SharedUI/Components/PaywallOverlay.swift
    Packages/SharedUI/Sources/SharedUI/Components/ReportsChangeLog.swift
    Packages/SharedUI/Sources/SharedUI/Components/TrackRow.swift
    Packages/SharedUI/Sources/SharedUI/Components/ReportsCharts.swift
  </files>
  <action>
**DesignTokens.swift:**
1. Add `Motion.curveLayout` token: `public static let curveLayout: Animation = .easeInOut(duration: 0.25)` -- for MainView column visibility animation (distinct from curveDefault at 0.3s). Add comment: `/// easeInOut 250ms -- layout column visibility changes.`
2. Confirm `motionAnimation` extension is still present -- do NOT remove it. Per user locked decision, it must be preserved.

**SharedUI.swift:**
1. Remove the stale comment `// Placeholder for Phase 1. Real content added in Phase 6.`
2. Replace with: `// SharedUI -- Shared SwiftUI components, themes, and design tokens.`

**ProgressRing.swift** (token consistency):
1. Replace `.secondary` (line ~39) with `Ayu.fgSecondary`
2. Replace `.primary` (line ~70) with `Ayu.fgPrimary`
3. Replace `.quaternary` (line ~52) with `Ayu.bgTertiary`
4. Replace `VStack(spacing: 12)` (line ~28) with `VStack(spacing: Spacing.sm)`
5. Replace `.easeInOut(duration: 0.4)` (line ~63) with `Motion.curveEmphasis`
6. Replace `VStack(spacing: 24)` (line ~105) with `VStack(spacing: Spacing.xl)`
7. Leave `.font(.system(.title3, design: .rounded))` as-is (unique to ProgressRing, no matching AppFont token)

**ConfidenceBadge.swift:**
1. Replace `spacing: 12` (line ~58) with `Spacing.sm`

**TierBadge.swift:**
1. Replace `spacing: 12` (line ~66) with `Spacing.sm`

**EmptyStateView.swift:**
1. Replace `spacing: 16` (line ~40) with `Spacing.md`

**PaywallOverlay.swift:**
1. Replace `spacing: 20` (line ~59) with `Spacing.lg`
2. Replace `spacing: 8` (line ~82) with `Spacing.xs`
3. Replace `spacing: 16` (line ~95) with `Spacing.md`
4. Replace `spacing: 12` (line ~113) with `Spacing.sm`
5. Leave `spacing: 2` and `spacing: 6` as raw -- sub-token values per RESEARCH recommendation

**ReportsChangeLog.swift:**
1. Replace `spacing: 12` (line ~48) with `Spacing.sm`
2. Replace `spacing: 4` (line ~126) with `Spacing.xxs`
3. Leave `spacing: 2` (line ~92) as raw

**TrackRow.swift:**
1. Replace `spacing: 4` (line ~21) with `Spacing.xxs`
2. Leave `spacing: 2` (line ~16) as raw

**ReportsCharts.swift:**
1. Replace `spacing: 24` (line ~77) with `Spacing.xl`
2. Replace `spacing: 16` (line ~89) with `Spacing.md`
3. Replace `spacing: 8` (lines ~125, ~157, ~224) with `Spacing.xs`

**Important:** Verify each line number by reading the file first. Line numbers in RESEARCH may have drifted since analysis. Read each file, find the actual raw literal, and replace.

**Do NOT replace:**
- `spacing: 0` (semantic -- no spacing)
- `spacing: 2` (sub-token, below `Spacing.xxs`)
- `spacing: 6` (sub-token, between `Spacing.xxs` and `Spacing.xs`)
  </action>
  <verify>
    `swift build --package-path Packages/SharedUI` succeeds. Run `swiftlint lint --strict Packages/SharedUI/Sources` and `swiftformat Packages/SharedUI/Sources --lint` -- zero violations.
  </verify>
  <done>All SharedUI raw spacing/animation literals replaced with tokens. ProgressRing uses Ayu colors. SharedUI.swift stale comment removed. Motion.curveLayout added to DesignTokens.</done>
</task>

<task type="auto">
  <name>Task 2: Replace raw literals in App target and run Periphery scan</name>
  <files>
    App/GenreUpdaterApp.swift
    App/Views/MainView.swift
    App/Views/UpdateWorkflowView.swift
    App/Views/SubscriptionView.swift
    App/Views/UpdateView.swift
    App/Views/Components/DryRunSummaryView.swift
    App/Views/Components/GenreMappingsEditor.swift
    App/Views/Components/AlbumCard.swift
  </files>
  <action>
**GenreUpdaterApp.swift:**
1. Replace `.easeInOut(duration: 0.3)` (line ~115) with `Motion.curveDefault`

**MainView.swift:**
1. Replace `.easeInOut(duration: 0.2)` (line ~82) with `Motion.curveFast`
2. Replace `withAnimation(.easeInOut(duration: 0.25))` (line ~217) with `withAnimation(Motion.curveLayout)`

**UpdateWorkflowView.swift:**
1. Replace `.easeInOut(duration: 0.2)` (line ~33) with `Motion.curveFast`
2. Leave `spacing: 0` as-is
3. Leave `spacing: 2` (line ~352) as raw

**SubscriptionView.swift:**
1. Replace `spacing: 24` (line ~23) with `Spacing.xl`
2. Replace `spacing: 12` (line ~45) with `Spacing.sm`
3. Replace `spacing: 16` (line ~79) with `Spacing.md`
4. Replace `spacing: 4` (line ~96) with `Spacing.xxs`
5. Replace `spacing: 8` (line ~129) with `Spacing.xs`
6. Replace `spacing: 12` (line ~165) with `Spacing.sm`

**UpdateView.swift:**
1. Replace `spacing: 8` (line ~97) with `Spacing.xs`
2. Replace `spacing: 24` (line ~140) with `Spacing.xl`
3. Replace `spacing: 12` (line ~198) with `Spacing.sm`
4. Replace `spacing: 4` (line ~255) with `Spacing.xxs`
5. Replace `spacing: 20` (line ~313) with `Spacing.lg`
6. Replace `spacing: 8` (line ~344) with `Spacing.xs`
7. Leave `spacing: 2` (line ~214) as raw

**DryRunSummaryView.swift:**
1. Replace `spacing: 12` (lines ~36, ~89, ~119, ~186) with `Spacing.sm`
2. Replace `spacing: 8` (line ~38) with `Spacing.xs`
3. Replace `spacing: 16` (line ~47) with `Spacing.md`
4. Replace `spacing: 4` (line ~163) with `Spacing.xxs`
5. Leave `spacing: 2` (lines ~75, ~122) as raw

**GenreMappingsEditor.swift:**
1. Replace `spacing: 8` (line ~73) with `Spacing.xs`

**AlbumCard.swift:**
1. Leave `spacing: 2` (line ~54) as raw

**Important:** Verify each line number by reading the file first. Line numbers in RESEARCH may have drifted. Read each file, find the actual raw literal, replace.

**Periphery scan:**
After all token replacements, run Periphery on SharedUI and App:
```bash
cd Packages/SharedUI && periphery scan --retain-public --retain-codable-properties 2>&1 | head -50
cd Packages/Core && periphery scan --retain-public --retain-codable-properties 2>&1 | head -50
```
Note: App target Periphery requires Xcode project. If findings exist, triage:
- Truly dead code (no consumers, no future use): remove
- Potentially useful (future phases): keep with `// periphery:ignore` comment
- Document findings in SUMMARY.
  </action>
  <verify>
    Full build: `xcodebuild build -project GenreUpdater.xcodeproj -scheme GenreUpdater -destination "platform=macOS,arch=arm64" CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO -quiet`. Run `swiftlint lint --strict App Packages/Core/Sources Packages/Services/Sources Packages/SharedUI/Sources` and `swiftformat App Packages/Core/Sources Packages/Services/Sources Packages/SharedUI/Sources --lint` -- zero violations. Run `swift test --package-path Packages/Core` -- all pass.
  </verify>
  <done>All App target raw literals replaced with tokens. Periphery scan completed and findings documented. Zero raw spacing/animation literals remain where a token match exists across the entire codebase.</done>
</task>

</tasks>

<verification>
1. `swift build --package-path Packages/SharedUI` passes
2. Full Xcode build passes (`xcodebuild build ... -quiet`)
3. `swiftlint lint --strict App Packages/Core/Sources Packages/Services/Sources Packages/SharedUI/Sources` returns 0 violations
4. `swiftformat App Packages/Core/Sources Packages/Services/Sources Packages/SharedUI/Sources --lint` returns 0 violations
5. `swift test --package-path Packages/Core` passes (no regressions)
6. Periphery scan completed with findings documented
7. Grep confirms no remaining raw spacing literals matching tokens: `rg "spacing: (4|8|12|16|20|24)" --glob "*.swift" App/ Packages/SharedUI/Sources/` returns zero matches (except sub-token values and intentional keeps)
</verification>

<success_criteria>
- Every `spacing: N` where N matches a Spacing token (4, 8, 12, 16, 20, 24, 32, 40, 48, 64) is replaced with the token equivalent
- Every `.easeInOut(duration: N)` where N matches a Motion duration (0.2, 0.25, 0.3, 0.4) is replaced with the token equivalent
- ProgressRing uses Ayu.fgSecondary, Ayu.fgPrimary, Ayu.bgTertiary instead of system colors
- SharedUI.swift has accurate comment (no stale Phase 1 reference)
- motionAnimation extension still exists in DesignTokens.swift
- Motion.curveLayout exists at 0.25s
- Periphery scan results documented
- Sub-token values (0, 2, 6) remain as raw literals (intentional)
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-dashboard-hotfix/05.1-02-SUMMARY.md`
</output>
