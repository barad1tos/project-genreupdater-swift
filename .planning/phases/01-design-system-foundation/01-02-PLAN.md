---
phase: 01-design-system-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/SharedUI/Sources/SharedUI/Theme/AyuColors.swift
  - App/GenreUpdaterApp.swift
autonomous: true
requirements:
  - DSYS-05

must_haves:
  truths:
    - "fgSecondary light-mode hex is #697078 — captions and placeholders pass WCAG AA (≥4.5:1) on all Ayu light surfaces"
    - "fgPrimary light-mode hex remains #5C6166 — it already passes at 6.10:1 and must NOT be changed"
    - "ContentView enforces minWidth: 900 (up from 800) preventing layout collapse"
    - "WindowGroup sets defaultSize(width: 1280, height: 800) for first-launch dimensions"
    - "SharedUI and App targets build with zero errors after changes"
  artifacts:
    - path: "Packages/SharedUI/Sources/SharedUI/Theme/AyuColors.swift"
      provides: "Fixed fgSecondary light-mode color"
      contains: "0x697078"
    - path: "App/GenreUpdaterApp.swift"
      provides: "Window size enforcement"
      contains: "minWidth: 900"
  key_links:
    - from: "AyuColors.swift fgSecondary"
      to: "WCAG AA threshold (4.5:1)"
      via: "hex #697078 (4.89:1 on bgPrimary, 4.55:1 on bgSecondary)"
      pattern: "0x697078"
    - from: "ContentView frame modifier"
      to: "minWidth: 900"
      via: ".frame(minWidth: 900"
      pattern: "minWidth: 900"
    - from: "WindowGroup"
      to: "defaultSize"
      via: ".defaultSize(width: 1280, height: 800)"
      pattern: "\\.defaultSize\\("
---

<objective>
Fix Ayu light-mode fgSecondary contrast to pass WCAG AA and enforce minimum/default window dimensions.

Purpose: WCAG AA compliance protects caption/placeholder readability in light mode. Window size enforcement prevents layout collapse at narrower widths (sidebar + browse panel requires ≥900pt).
Output: Updated AyuColors.swift (one hex value changed) and GenreUpdaterApp.swift (two frame values updated).
</objective>

<execution_context>
@/Users/cloud/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cloud/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation/01-RESEARCH.md
@Packages/SharedUI/Sources/SharedUI/Theme/AyuColors.swift
@App/GenreUpdaterApp.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix fgSecondary light-mode hex in AyuColors.swift</name>
  <files>Packages/SharedUI/Sources/SharedUI/Theme/AyuColors.swift</files>
  <action>
Read AyuColors.swift first. Locate the `fgSecondary` declaration:

```swift
public static let fgSecondary = Color.adaptive(
    light: hex(0x8A9199),  // FAILS: 3.11:1 on bgPrimary
    dark: hex(0x8A9199)
)
```

Change ONLY the light hex value from `0x8A9199` to `0x697078`. Dark mode value stays `0x8A9199`:

```swift
public static let fgSecondary = Color.adaptive(
    light: hex(0x697078),  // 4.89:1 on bgPrimary, 4.55:1 on bgSecondary — PASSES WCAG AA
    dark: hex(0x8A9199)
)
```

CRITICAL — do NOT touch:
- `fgPrimary` (0x5C6166) — already passes at 6.10:1 (STATE.md blocker was stale; research confirmed passing)
- Any dark mode hex values
- Any other color tokens
- File structure, spacing, or comments

After the edit, read the file back to confirm exactly one hex was changed.
  </action>
  <verify>
Run: `grep -A3 "fgSecondary" Packages/SharedUI/Sources/SharedUI/Theme/AyuColors.swift`

Expected output contains `0x697078` for light and `0x8A9199` for dark.
Run: `swift build --package-path Packages/SharedUI 2>&1 | tail -5`
Expected: exits 0.
  </verify>
  <done>
- fgSecondary light value is 0x697078
- fgSecondary dark value is unchanged (0x8A9199)
- fgPrimary light value is unchanged (0x5C6166)
- SharedUI builds cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce 900pt minimum width and 1280x800 default size in GenreUpdaterApp.swift</name>
  <files>App/GenreUpdaterApp.swift</files>
  <action>
Read GenreUpdaterApp.swift first. Two changes needed:

CHANGE 1 — ContentView frame minimum width (in ContentView.body):
Locate: `.frame(minWidth: 800, minHeight: 600)`
Replace with: `.frame(minWidth: 900, minHeight: 600)`

CHANGE 2 — WindowGroup default size (in GenreUpdaterApp.body, after the closing brace of WindowGroup content):
The WindowGroup currently has `.commands { ... }` and `.onChange(of: scenePhase)` modifiers.
Add `.defaultSize(width: 1280, height: 800)` as the FIRST modifier on WindowGroup, immediately after the closing `}` of the WindowGroup content block, before `.commands`:

```swift
WindowGroup {
    ContentView()
        .environment(dependencies)
        .optionalModelContainer(dependencies.modelContainer)
        .task {
            await dependencies.initialize()
        }
}
.defaultSize(width: 1280, height: 800)
.commands {
    // ... existing commands unchanged
}
```

Do NOT change:
- SettingsView frame (`.frame(minWidth: 520, ...)`) — this is for the Settings window only
- Any command or navigation logic
- The `onChange` modifier

After editing, read the file back to verify both changes landed correctly.
  </action>
  <verify>
Run: `grep -n "minWidth" App/GenreUpdaterApp.swift`
Expected: ContentView frame shows `minWidth: 900`, Settings frame shows `minWidth: 520`.

Run: `grep -n "defaultSize" App/GenreUpdaterApp.swift`
Expected: `.defaultSize(width: 1280, height: 800)` appears once.

Run: `xcodebuild build -project GenreUpdater.xcodeproj -scheme GenreUpdater -destination "platform=macOS,arch=arm64" CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO -quiet 2>&1 | tail -10`
Expected: `** BUILD SUCCEEDED **`
  </verify>
  <done>
- ContentView .frame has minWidth: 900 (not 800)
- WindowGroup has .defaultSize(width: 1280, height: 800)
- Settings frame is unchanged at 520
- Full Xcode build succeeds
  </done>
</task>

</tasks>

<verification>
1. `grep "0x697078" Packages/SharedUI/Sources/SharedUI/Theme/AyuColors.swift` returns a match
2. `grep "0x5C6166" Packages/SharedUI/Sources/SharedUI/Theme/AyuColors.swift` returns a match (unchanged)
3. `grep "minWidth: 900" App/GenreUpdaterApp.swift` returns a match
4. `grep "defaultSize" App/GenreUpdaterApp.swift` returns a match
5. `swift build --package-path Packages/SharedUI` exits 0
6. Full Xcode build succeeds with CODE_SIGN_IDENTITY="-"
7. SwiftLint passes: `swiftlint lint --strict App Packages/SharedUI/Sources`
</verification>

<success_criteria>
- fgSecondary light-mode is #697078 (4.89:1 on bgPrimary — WCAG AA pass)
- fgPrimary remains #5C6166 (unchanged — already passing)
- Window minimum width is 900pt (prevents layout collapse)
- WindowGroup default size is 1280x800 (generous first-launch)
- Both packages and App target build cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-02-SUMMARY.md` with:
- What was changed (exact hex values before/after, exact frame values before/after)
- Contrast ratios confirmed for fixed color
- Build verification result
- Any deviations from the plan
</output>
