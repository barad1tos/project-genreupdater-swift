name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions: {}

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  SERVICES_BUILD_TIMEOUT: 5
  SERVICES_TEST_RUNTIME_TIMEOUT_SECONDS: 120
  SERVICES_TEST_STEP_TIMEOUT: 3
  SERVICES_TEST_TERMINATION_GRACE_SECONDS: 5

jobs:
  build-and-lint:
    name: Build, Test & Lint
    runs-on: macos-15
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          persist-credentials: false

      - name: Cache SPM packages
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5
        with:
          path: |
            Packages/Core/.build
            Packages/Services/.build
            Packages/SharedUI/.build
          key: spm-${{ runner.os }}-${{ hashFiles('Packages/*/Package.swift', 'Packages/*/Package.resolved') }}
          restore-keys: |
            spm-${{ runner.os }}-

      - name: Remove unused Homebrew tap
        run: |
          TAP_PATH="$(brew --repository)/Library/Taps/aws/homebrew-tap"
          if [[ -d "$TAP_PATH" ]]; then
            HOMEBREW_NO_REQUIRE_TAP_TRUST=1 brew untap aws/tap
          fi

      - name: Print Swift version
        run: swift --version

      - name: Test fixture manifest tooling
        run: python3 -B scripts/test-fixture-manifest.py

      - name: Build Core
        run: swift build --package-path Packages/Core

      - name: Build Services
        run: swift build --package-path Packages/Services

      - name: Build SharedUI
        run: swift build --package-path Packages/SharedUI

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Xcode app gate
        run: bash scripts/xcode-gate.sh
        timeout-minutes: 15

      - name: Unlock Keychain for tests
        run: security unlock-keychain -p "" ~/Library/Keychains/login.keychain-db

      - name: Clear cached coverage data
        run: |
          for build_path in Packages/Core/.build Packages/Services/.build; do
            if [[ -d "$build_path" ]]; then
              find "$build_path" -type d -name codecov -prune -exec rm -rf {} +
            fi
          done

      - name: Generate and verify the V1 store fixture
        timeout-minutes: 5
        run: |
          fixture_directory="$(mktemp -d)"
          trap 'rm -rf -- "${fixture_directory}"' EXIT
          generated_fixture="${fixture_directory}/StoreSchemaV1.fixture"
          bash Packages/Services/Tests/LegacyFixtures/generate-v1-store.sh "${generated_fixture}"
          GENREUPDATER_V1_FIXTURE_PATH="${generated_fixture}" swift test \
            --package-path Packages/Services \
            --disable-automatic-resolution \
            --filter StoreSchemaMigrationTests.migratesMirrorScopeStore

      - name: Test Core
        run: swift test --package-path Packages/Core --enable-code-coverage
        timeout-minutes: 5

      - name: Build Services tests with coverage
        run: swift build --package-path Packages/Services --build-tests --enable-code-coverage
        timeout-minutes: ${{ fromJSON(env.SERVICES_BUILD_TIMEOUT) }}

      - name: Test Services
        run: |
          set -m
          services_bin_path=$(
            swift build --package-path Packages/Services \
              --configuration debug --disable-automatic-resolution --show-bin-path
          )
          coverage_fragments_directory="${RUNNER_TEMP}/services-codecov"
          coverage_output="${services_bin_path}/codecov/default.profdata"
          mkdir -p "${coverage_fragments_directory}" "$(dirname "${coverage_output}")"
          test_log="${RUNNER_TEMP}/services-tests.log"

          merge_services_coverage() {
            local -a profile_fragments
            shopt -s nullglob
            profile_fragments=("${coverage_fragments_directory}/"*.profraw)
            if (( ${#profile_fragments[@]} == 0 )); then
              echo "::error::Services tests produced no coverage fragments"
              return 1
            fi
            xcrun llvm-profdata merge --sparse "${profile_fragments[@]}" \
              --output="${coverage_output}"
          }

          # %c enables Darwin continuous mode so signal termination preserves coverage counters.
          LLVM_PROFILE_FILE="${coverage_fragments_directory}/services-%p%c.profraw" \
            NSUnbufferedIO=YES \
            swift test --package-path Packages/Services --skip-build \
            >"${test_log}" 2>&1 &
          test_pid=$!
          SECONDS=0

          while kill -0 "${test_pid}" 2>/dev/null; do
            if (( SECONDS >= SERVICES_TEST_RUNTIME_TIMEOUT_SECONDS )); then
              kill -- "-${test_pid}" 2>/dev/null || true
              termination_deadline=$((SECONDS + SERVICES_TEST_TERMINATION_GRACE_SECONDS))
              while kill -0 -- "-${test_pid}" 2>/dev/null \
                && (( SECONDS < termination_deadline )); do
                sleep 1
              done
              kill -KILL -- "-${test_pid}" 2>/dev/null || true
              wait "${test_pid}" 2>/dev/null || true
              cat "${test_log}"

              if grep -Fq "Test Suite 'All tests' passed" "${test_log}" \
                && grep -Eq 'Test run with [0-9]+ tests?.*passed' "${test_log}"; then
                merge_services_coverage
                echo "::warning::Services tests passed but the SwiftData process did not exit"
                exit 0
              fi

              echo "::error::Services tests did not complete within ${SERVICES_TEST_RUNTIME_TIMEOUT_SECONDS}s"
              exit 1
            fi
            sleep 1
          done

          if wait "${test_pid}"; then
            test_status=0
          else
            test_status=$?
          fi
          cat "${test_log}"
          if (( test_status != 0 )); then
            exit "${test_status}"
          fi
          merge_services_coverage
        timeout-minutes: ${{ fromJSON(env.SERVICES_TEST_STEP_TIMEOUT) }}

      # Run UI package tests with coverage instrumentation. Thresholds remain
      # deferred because current coverage is too low to provide a useful floor.
      - name: Test SharedUI
        run: swift test --package-path Packages/SharedUI --enable-code-coverage
        timeout-minutes: 5

      - name: Test DesignUI
        run: swift test --package-path Packages/DesignUI --enable-code-coverage
        timeout-minutes: 5

      - name: Check coverage thresholds
        run: |
          FAILED=0

          # Core coverage (threshold: 85%)
          echo "=== Core coverage ==="
          CORE_BIN_PATH=$(
            swift build --package-path Packages/Core \
              --configuration debug --disable-automatic-resolution --show-bin-path
          )
          CORE_BINARY="$CORE_BIN_PATH/CorePackageTests.xctest/Contents/MacOS/CorePackageTests"
          CORE_PROFDATA="$CORE_BIN_PATH/codecov/default.profdata"
          if [ -f "$CORE_PROFDATA" ] && [ -f "$CORE_BINARY" ]; then
            xcrun llvm-cov report "$CORE_BINARY" \
              -instr-profile "$CORE_PROFDATA" \
              --ignore-filename-regex="Tests/" --ignore-filename-regex="\.build/" | tail -3
            CORE_COV=$(xcrun llvm-cov export "$CORE_BINARY" \
              -instr-profile "$CORE_PROFDATA" --summary-only \
              --ignore-filename-regex="Tests/" --ignore-filename-regex="\.build/" 2>/dev/null \
              | python3 -c "import sys,json;d=json.load(sys.stdin);print(int(d['data'][0]['totals']['lines']['percent']))")
            echo "Core line coverage: ${CORE_COV}% (threshold: 85%)"
            if [ "$CORE_COV" -lt 85 ]; then
              echo "::error::Core coverage ${CORE_COV}% is below threshold 85%"
              FAILED=1
            fi
          else
            echo "::error::Core coverage data not found"
            FAILED=1
          fi

          # Services coverage (threshold: 65%)
          # Excludes Core/ sources (tested separately) and system-dependent files
          echo "=== Services coverage ==="
          SERVICES_BIN_PATH=$(
            swift build --package-path Packages/Services \
              --configuration debug --disable-automatic-resolution --show-bin-path
          )
          SERVICES_BINARY="$SERVICES_BIN_PATH/ServicesPackageTests.xctest/Contents/MacOS/ServicesPackageTests"
          SERVICES_PROFDATA="$SERVICES_BIN_PATH/codecov/default.profdata"
          if [ -f "$SERVICES_PROFDATA" ] && [ -f "$SERVICES_BINARY" ]; then
            xcrun llvm-cov report "$SERVICES_BINARY" \
              -instr-profile "$SERVICES_PROFDATA" \
              --ignore-filename-regex="Tests/" --ignore-filename-regex="\.build/" --ignore-filename-regex="Core/" | tail -3
            SVC_COV=$(xcrun llvm-cov export "$SERVICES_BINARY" \
              -instr-profile "$SERVICES_PROFDATA" --summary-only \
              --ignore-filename-regex="Tests/" --ignore-filename-regex="\.build/" --ignore-filename-regex="Core/" 2>/dev/null \
              | python3 -c "import sys,json;d=json.load(sys.stdin);print(int(d['data'][0]['totals']['lines']['percent']))")
            echo "Services line coverage: ${SVC_COV}% (threshold: 65%)"
            if [ "$SVC_COV" -lt 65 ]; then
              echo "::error::Services coverage ${SVC_COV}% is below threshold 65%"
              FAILED=1
            fi
          else
            echo "::error::Services coverage data not found"
            FAILED=1
          fi

          if [ "$FAILED" -ne 0 ]; then
            exit 1
          fi
          echo "All coverage thresholds passed"

      - name: Validate entitlements
        run: bash scripts/validate-entitlements.sh

      - name: Install lint tools
        env:
          PERIPHERY_SHA256: 07d4e286e31dd79164df39097e0b59f533c94badbe18158464a455ea88a166d7
          PERIPHERY_VERSION: 3.8.0
        run: |
          brew install swiftlint swiftformat
          PERIPHERY_DIRECTORY="$RUNNER_TEMP/periphery-${PERIPHERY_VERSION}"
          PERIPHERY_ARCHIVE="${PERIPHERY_DIRECTORY}.zip"
          curl --fail --location --proto '=https' --proto-redir '=https' --silent --show-error \
            --output "$PERIPHERY_ARCHIVE" \
            "https://github.com/peripheryapp/periphery/releases/download/${PERIPHERY_VERSION}/periphery-${PERIPHERY_VERSION}.zip"
          echo "${PERIPHERY_SHA256}  ${PERIPHERY_ARCHIVE}" | shasum --algorithm 256 --check
          mkdir -p "$PERIPHERY_DIRECTORY"
          unzip -q "$PERIPHERY_ARCHIVE" -d "$PERIPHERY_DIRECTORY"
          echo "$PERIPHERY_DIRECTORY" >> "$GITHUB_PATH"

      - name: Run SwiftLint
        run: >
          swiftlint lint --strict
          App
          Agent
          Packages/Core/Sources
          Packages/DesignUI/Sources
          Packages/Services/Sources
          Packages/SharedUI/Sources

      - name: Run SwiftFormat (check)
        run: >
          swiftformat
          App
          Agent
          Packages/Core/Sources
          Packages/DesignUI/Sources
          Packages/Services/Sources
          Packages/SharedUI/Sources
          --lint

      # Scan the application scheme so Periphery analyzes App, Agent, and
      # package sources. Do not retain all public declarations because
      # redundant package accessibility is part of the ratchet.
      #
      # A ratchet, not a clean bill: unused declarations and redundant public
      # access are separate debt categories, so neither can hide growth in the
      # other. Every warning stays visible; an unknown category fails closed.
      - name: Test Periphery report gate
        run: bash scripts/test-periphery-report.sh

      - name: Scan for unused code (Periphery)
        run: |
          # Include the real, non-test consumer of the frozen V1 schema. The
          # executable remains separate from the app while Periphery analyzes
          # both production index stores in one census.
          SERVICES_SCRATCH_PATH=.build/periphery-services
          swift build \
            --package-path Packages/Services \
            --scratch-path "${SERVICES_SCRATCH_PATH}" \
            --disable-automatic-resolution \
            --target StoreFixtureGenerator
          SERVICES_BIN_PATH="$(
            swift build \
              --package-path Packages/Services \
              --scratch-path "${SERVICES_SCRATCH_PATH}" \
              --disable-automatic-resolution \
              --show-bin-path
          )"

          # Findings exit zero without --strict; only a nonzero status means
          # the scan itself failed, which the validator must reject.
          set +e
          periphery scan \
            --project GenreUpdater.xcodeproj \
            --schemes GenreUpdater \
            --skip-build \
            --index-store-path .build/xcode/Index.noindex/DataStore \
            --index-store-path "${SERVICES_BIN_PATH}/index/store" \
            --retain-codable-properties \
            --retain-swift-ui-previews \
            --retain-assign-only-properties \
            --retain-objc-accessible \
            --exclude-tests \
            --disable-update-check \
            --verbose \
            --write-results periphery-report.txt \
            > periphery-scan.log 2>&1
          SCAN_STATUS=$?
          set -e
          bash scripts/validate-periphery-report.sh \
            .periphery-baseline \
            periphery-report.txt \
            periphery-scan.log \
            "$SCAN_STATUS"

  gate:
    name: CI Gate
    runs-on: ubuntu-latest
    needs: [build-and-lint]
    if: always()
    permissions: {}

    steps:
      - name: Check all jobs passed
        run: |
          if [[ "${{ needs.build-and-lint.result }}" != "success" ]]; then
            echo "::error::build-and-lint failed with status: ${{ needs.build-and-lint.result }}"
            exit 1
          fi
          echo "All CI checks passed"
